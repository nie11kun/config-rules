#!name=marco-marco-frp-proxy
#!desc=personal use frp http-proxy

[Rule]
DOMAIN-SUFFIX,home.marco,ESXI-HTTP_PROXY

# 如果不能配置路由器，windows 下需配置 hosts 使自定义 URL 指向本机本地 IP
# 127.0.0.1 pc.work.marco
# 然后刷新 DNS：ipconfig /flushdns
DOMAIN-SUFFIX,work.marco,XPS15-HTTP_PROXY
